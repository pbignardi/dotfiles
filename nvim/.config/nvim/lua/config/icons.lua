-- defines a globa icon table, sets some common icons and returns the table
-- in particular, these icons can be used as an extension to mini.icons
-- especially for whichkey plugin

local icons = {
	error = "󰀩",
	warn = "",
	info = "",
	hint = "",
	lsp = "󰙅",
}

return icons
